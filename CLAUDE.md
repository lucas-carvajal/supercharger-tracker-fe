@AGENTS.md

# Claude-Specific Notes

Use `AGENTS.md` as the canonical project guide. The docs index, admin auth notes, environment variable guidance, and privacy/terms reminder all live there.

When running the app through Claude preview, use the existing `.claude/launch.json` `next-dev` configuration.
