@AGENTS.md

# After every change

Check whether the change affects what data is collected, how external services are used, or what guarantees are made about data accuracy or uptime. If so, update the relevant sections in `app/privacy/page.tsx` and/or `app/terms/page.tsx` and bump the "Last updated" date.
